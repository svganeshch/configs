<?php
   define('DEVICES_LIST_URL', ''); // Official devices list
   define('DB_SERVER', 'localhost:3306'); // DB server url. Usually localhost:3306
   define('DB_USERNAME', 'username'); // DB username
   define('DB_PASSWORD', 'password'); // DB password
   define('DB_DATABASE', 'database_name'); // DB to use
   $db = mysqli_connect(DB_SERVER,DB_USERNAME,DB_PASSWORD,DB_DATABASE);
   if (!$db) {
	die("Database Connection Failed" . mysqli_error($db));
   }
?>
